<%= simple_form_for(@sale) do |f| %>
  <% if @sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @sale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.input :title %>
  <%= f.input :discount %>
<%= f.input :active %>

  <div class="actions">
    <br />
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>